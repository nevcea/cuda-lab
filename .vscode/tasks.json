{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build & run current .cu",
      "type": "shell",
      "command": "uv run ${workspaceFolder}/build.py ${fileBasenameNoExtension}",
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": []
    }
  ]
}
